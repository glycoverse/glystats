% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-test.R
\name{gly_correlated_sets}
\alias{gly_correlated_sets}
\title{Construct Correlated Variable Sets}
\usage{
gly_correlated_sets(
  exp,
  threshold = 0.9,
  correlation = "pearson",
  clustering = "complete",
  min_size = 2L,
  within = NULL
)
}
\arguments{
\item{exp}{A \code{\link[glyexp:GlycomicSE]{glyexp::GlycomicSE()}} or \code{\link[glyexp:GlycoproteomicSE]{glyexp::GlycoproteomicSE()}} object,
or another \code{SummarizedExperiment} containing expression data and variable
information.}

\item{threshold}{A numeric correlation threshold between 0 and 1. Variables
are linked only when their correlation is strictly greater than this
value. Default is \code{0.9}.}

\item{correlation}{Correlation coefficient to use. Either \code{"pearson"}
(default) or \code{"spearman"}.}

\item{clustering}{How correlated variables are grouped. \code{"complete"}
(default) uses complete-linkage clustering so every pair in a set exceeds
\code{threshold}. \code{"connected"} uses connected components and therefore allows
transitive chaining.}

\item{min_size}{Minimum number of distinct profiles in a set. Default is 2.}

\item{within}{Optional variable-information columns defining strata within
which sets are constructed, such as \code{c("protein", "protein_site")}.}
}
\value{
An object of class \code{glystats_correlated_sets} containing:
\itemize{
\item \code{sets}: A named list mapping set identifiers to all member variables.
\item \code{membership}: A tibble containing set, variable, representative, and alias
information.
\item \code{representatives}: The distinct profiles used to define each set.
\item \code{correlation_matrix}: The pooled-sample correlation matrix. Correlations
across different \code{within} strata are \code{NA}.
\item \code{excluded_variables}: A tibble of excluded variables and reasons.
\item \code{aliases}: A named list mapping representative variables to aliases.
\item Construction settings and metadata copied from \code{exp}.
}
}
\description{
Group highly correlated variables into sets for covariance-aware differential
testing with \code{\link[=gly_set_test]{gly_set_test()}}.
}
\details{
Correlations are calculated on \code{log2(x + 1e-6)} values, matching the scale
used by \code{\link[=gly_set_test]{gly_set_test()}} and other differential-testing functions in this
package. All-missing, insufficiently observed, and zero-variance variables
are excluded. Numerically equivalent profiles are represented once during
clustering and testing, while their aliases remain in the returned set
membership.

\code{clustering = "connected"} reproduces the transitive set-construction rule
used by glycowork. \code{clustering = "complete"} avoids chaining by requiring
all within-set correlations to exceed \code{threshold}.
}
\examples{
set.seed(1)
expression <- matrix(
  stats::rexp(36),
  nrow = 3,
  dimnames = list(c("A", "B", "C"), paste0("S", 1:12))
)
expression["B", ] <- expression["A", ] * exp(stats::rnorm(12, sd = 0.01))
exp <- SummarizedExperiment::SummarizedExperiment(
  assays = list(expression = expression),
  colData = S4Vectors::DataFrame(
    group = factor(rep(c("control", "case"), each = 6))
  )
)
sets <- gly_correlated_sets(exp, threshold = 0.8)
sets$membership

}
\seealso{
\code{\link[=gly_set_test]{gly_set_test()}}, \code{\link[stats:cor]{stats::cor()}}, \code{\link[stats:hclust]{stats::hclust()}}
}
