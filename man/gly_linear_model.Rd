% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear-model.R
\name{gly_linear_model}
\alias{gly_linear_model}
\title{Formula-based Linear Models}
\usage{
gly_linear_model(
  exp,
  formula,
  contrasts = NULL,
  p_adj_method = "BH",
  add_info = TRUE,
  ...
)
}
\arguments{
\item{exp}{A \code{\link[glyexp:GlycomicSE]{glyexp::GlycomicSE()}} or \code{\link[glyexp:GlycoproteomicSE]{glyexp::GlycoproteomicSE()}} object,
or another \code{SummarizedExperiment} containing expression data and sample
information.}

\item{formula}{A one-sided formula describing the model in terms of columns
in the sample information, for example \code{~ treatment * time + batch}.}

\item{contrasts}{An optional uniquely named character vector of limma-style
contrast expressions. Use backticks around non-syntactic coefficient names,
such as \code{`treatmentB:timeT2`}.}

\item{p_adj_method}{A character string specifying the method used to adjust
p-values within each coefficient or contrast. See \code{\link[stats:p.adjust]{stats::p.adjust()}} for
available methods. If \code{NULL}, no adjustment is performed.}

\item{add_info}{A logical value. If \code{TRUE} (default), variable information
from the experiment is added to the tidy result.}

\item{...}{Additional arguments passed to \code{\link[limma:lmFit]{limma::lmFit()}}.}
}
\value{
A list with classes \code{glystats_linear_model_res} and \code{glystats_res}
containing:
\itemize{
\item \code{tidy_result}: A tibble with \code{variable}, \code{term}, \code{estimate}, \code{AveExpr},
\code{t}, \code{p_val}, optional \code{p_adj}, and \code{b} columns.
\item \code{raw_result}: A list containing the moderated limma fit, design matrix,
contrast matrix, and coefficient-name mapping.
\item \code{meta_data}: Metadata copied from \code{exp}.
}
}
\description{
Fit a linear model to every variable in a glycomics or glycoproteomics
experiment using limma's empirical Bayes moderation.
}
\details{
Expression values are transformed using \code{log2(x + 1e-6)} before modeling.
When \code{contrasts} is \code{NULL}, all non-intercept coefficients are tested. When
contrasts are supplied, only the named contrasts are returned.

Character predictors are handled as factors by \code{\link[stats:model.matrix]{stats::model.matrix()}}.
Factor reference levels therefore determine the meaning of coefficients.
Set factor levels in the sample information before calling this function when
a specific reference level is required.

P-values are adjusted independently across variables for each coefficient or
contrast.
}
\examples{
\dontshow{if (requireNamespace("limma", quietly = TRUE)) withAutoprint(\{ # examplesIf}
exp <- glyexp::real_experiment
complete <- rowSums(is.finite(SummarizedExperiment::assay(exp))) == ncol(exp)
exp <- exp[which(complete)[seq_len(10)], ]
SummarizedExperiment::colData(exp)$time <- factor(
  rep(c("early", "late"), length.out = ncol(exp))
)

model_res <- gly_linear_model(exp, ~ group * time)

contrast_res <- gly_linear_model(
  exp,
  ~ group * time,
  contrasts = c(
    C_late_vs_H_late = "groupC + `groupC:timelate`"
  )
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=gly_limma]{gly_limma()}}, \code{\link[limma:lmFit]{limma::lmFit()}}, \code{\link[limma:ebayes]{limma::eBayes()}}
}
