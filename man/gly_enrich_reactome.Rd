% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.R
\name{gly_enrich_reactome}
\alias{gly_enrich_reactome}
\alias{gly_enrich_reactome_}
\title{Reactome pathway over-representation analysis (ORA)}
\usage{
gly_enrich_reactome(
  exp,
  add_info = TRUE,
  orgdb = "org.Hs.eg.db",
  organism = "human",
  universe = NULL,
  p_adj_method = "BH",
  p_cutoff = 0.05,
  q_cutoff = 0.2
)

gly_enrich_reactome_(
  proteins,
  orgdb = "org.Hs.eg.db",
  organism = "human",
  universe = NULL,
  p_adj_method = "BH",
  p_cutoff = 0.05,
  q_cutoff = 0.2
)
}
\arguments{
\item{exp}{(Only for \code{\link[=gly_enrich_reactome]{gly_enrich_reactome()}}) A \code{glyexp::experiment()} object.}

\item{add_info}{A logical value. This parameter is included for API consistency but has no effect
since enrichment results do not contain variable or sample columns.
Only applicable to top-level APIs.}

\item{orgdb}{Passed to \code{OrgDb} of \code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}}.
Organism database name (e.g., "org.Hs.eg.db" for human). Defaults to "org.Hs.eg.db".}

\item{organism}{Passed to \code{organism} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.
Species name (e.g., "human", "mouse", "rat"). Defaults to "human".}

\item{universe}{Background genes. If a character vector, directly passed to \code{universe} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.
You can also provide a \code{\link[glyexp:experiment]{glyexp::experiment()}} object with "glycoproteomics" type.
In this case all detected proteins in this experiment will be extracted and passed to
\code{universe} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.}

\item{p_adj_method}{Passed to \code{pAdjustMethod} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.}

\item{p_cutoff}{Passed to \code{pvalueCutoff} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.}

\item{q_cutoff}{Passed to \code{qvalueCutoff} of \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.}

\item{proteins}{(Only for \code{\link[=gly_enrich_reactome_]{gly_enrich_reactome_()}}) A character vector of UniProt accession IDs.}
}
\value{
A list with three elements:
\itemize{
\item \code{tidy_result}: A tibble with enrichment results containing the following columns:
\itemize{
\item \code{id}: Term ID (e.g., R-HSA-XXXXXX)
\item \code{description}: Term description
\item \code{gene_ratio}: Ratio of genes in the term to total genes in the input
\item \code{bg_ratio}: Ratio of genes in the term to total genes in the background
\item \code{p_val}: Raw p-value from hypergeometric test
\item \code{p_adj}: Adjusted p-value
\item \code{q_value}: Q-value (FDR)
\item \code{gene_id}: Gene IDs in the term (separated by "/")
\item \code{count}: Number of genes in the term
}
\item \code{raw_result}: The raw ReactomePA enrichPathway result object
\item \code{meta_data} (only for \code{\link[=gly_enrich_reactome]{gly_enrich_reactome()}}): A list containing metadata from the input experiment
The list has classes \code{glystats_reactome_ora_res} and \code{glystats_res}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

This function was deprecated because we decided to move all enrichment analysis functions
to the separate \code{glyfun} package, which has more features and better API design.

Perform Reactome pathway ORA for protein UniProt accessions using \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}.
\itemize{
\item \code{gly_enrich_reactome()} accepts a \code{\link[glyexp:experiment]{glyexp::experiment()}} and extracts protein information
from the "protein" column in the variable information tibble.
\item \code{gly_enrich_reactome_()} accepts a character vector of UniProt IDs.
}

As \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}} only accepts Entrez IDs,
the UniProt IDs will be first transformed into Entrez IDs with \code{\link[clusterProfiler:bitr]{clusterProfiler::bitr()}}.
}
\section{Required packages}{

These functions require the following packages to be installed:
\itemize{
\item \code{clusterProfiler} for ID conversion
\item \code{ReactomePA} for enrichment analysis
\item \code{org.Hs.eg.db} for human gene annotation or other OrgDb packages
}
}

\seealso{
\code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}
}
\keyword{internal}
