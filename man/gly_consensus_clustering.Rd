% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc.R
\name{gly_consensus_clustering}
\alias{gly_consensus_clustering}
\alias{gly_consensus_clustering_}
\title{Consensus Clustering for Glycomics and Glycoproteomics Data}
\usage{
gly_consensus_clustering(
  exp,
  on = "sample",
  max_k = 9,
  reps = 1000,
  p_item = 0.8,
  cluster_alg = "hc",
  scale = TRUE,
  output_file = NULL,
  add_info = TRUE,
  ...
)

gly_consensus_clustering_(
  expr_mat,
  on = "sample",
  max_k = 9,
  reps = 1000,
  p_item = 0.8,
  cluster_alg = "hc",
  scale = TRUE,
  output_file = NULL,
  ...
)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} object containing expression matrix and sample information.}

\item{on}{A character string specifying what to cluster. Either "sample" (default) to cluster
samples/observations, or "variable" to cluster variables/features.}

\item{max_k}{Maximum number of clusters to test. Default is 9.
Passed to the \code{maxK} argument of \code{ConsensusClusterPlus::ConsensusClusterPlus()}.}

\item{reps}{Number of resampling iterations. Default is 1000.
Passed to the \code{reps} argument of \code{ConsensusClusterPlus::ConsensusClusterPlus()}.}

\item{p_item}{Proportion of items to sample. Default is 0.8.
Passed to the \code{pItem} argument of \code{ConsensusClusterPlus::ConsensusClusterPlus()}.}

\item{cluster_alg}{Clustering algorithm to use. Default is "hc" (hierarchical clustering).
Other options include "km" (k-means), "pam" (partitioning around medoids).
Passed to the \code{clusterAlg} argument of \code{ConsensusClusterPlus::ConsensusClusterPlus()}.}

\item{scale}{A logical indicating whether to scale the data. Default is TRUE.}

\item{output_file}{A character string specifying the output file path for plots.
If NULL (default), no plots are saved and no files are generated. If provided,
the consensus clustering plot will be saved directly to this file path.
The function will create any necessary parent directories.}

\item{add_info}{A logical value. If TRUE (default), sample and variable information from the experiment
will be added to the result tibbles. If FALSE, only the consensus clustering results are returned.
Only applicable to \code{gly_consensus_clustering()}.}

\item{...}{Additional arguments passed to \code{ConsensusClusterPlus::ConsensusClusterPlus()}.}

\item{expr_mat}{A numeric matrix with variables as rows and samples as columns.}
}
\value{
A list with two elements:
\itemize{
\item \code{tidy_result}: A tibble with consensus clustering results containing the following columns:
\itemize{
\item \code{variable} or \code{sample}: Variable or sample name (depending on \code{on} parameter)
\item \code{k}: Number of clusters
\item \code{cluster}: Cluster assignment for the corresponding k
}
\item \code{raw_result}: The raw ConsensusClusterPlus object
The list has classes \code{glystats_cc_res} and \code{glystats_res}.
}
}
\description{
Perform consensus clustering on the expression data using ConsensusClusterPlus.
The function returns cluster assignments for all k values from 2 to max_k in long format.
Setting \code{output_file} to visualize consensus CDF curves and consensus matrices,
which helps you decide the optimal number of clusters.
See \href{https://bioconductor.org/packages/release/bioc/vignettes/ConsensusClusterPlus/inst/doc/ConsensusClusterPlus.pdf}{this tutorial}
for more information.
}
\details{
The function performs log2 transformation on the expression data (log2(x + 1)) before
consensus clustering. When \code{on = "sample"} (default), samples are clustered based
on their expression profiles across variables. When \code{on = "variable"}, variables are clustered based on their
expression patterns across samples.

\code{gly_consensus_clustering()} is the top-level API that works with \code{glyexp::experiment()} objects and supports
the \code{add_info} parameter for joining experiment metadata.

\code{gly_consensus_clustering_()} is the underlying API that works with matrices directly,
providing more flexibility for users who don't use the glyexp package.

\strong{Consensus Clustering Process:}
\enumerate{
\item Perform consensus clustering for k = 2 to max_k using ConsensusClusterPlus
\item Return cluster assignments for all k values in long format
}
}
\section{Required packages}{

This function requires the following packages to be installed:
\itemize{
\item \code{ConsensusClusterPlus} for consensus clustering
}
}

\seealso{
\code{\link[ConsensusClusterPlus:ConsensusClusterPlus]{ConsensusClusterPlus::ConsensusClusterPlus()}}
}
