% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wgcna.R
\name{gly_wgcna}
\alias{gly_wgcna}
\alias{gly_wgcna_}
\title{Weighted Gene Co-expression Network Analysis (WGCNA)}
\usage{
gly_wgcna(
  exp,
  powers = c(1:10, seq(12, 20, by = 2)),
  network_type = "unsigned",
  tom_type = "unsigned",
  min_module_size = 30,
  deep_split = 2,
  merge_cut_height = 0.25,
  add_info = TRUE,
  ...
)

gly_wgcna_(
  expr_mat,
  powers = c(1:10, seq(12, 20, by = 2)),
  network_type = "unsigned",
  tom_type = "unsigned",
  min_module_size = 30,
  deep_split = 2,
  merge_cut_height = 0.25,
  ...
)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} object.}

\item{powers}{A numeric vector of soft thresholding powers to test.
Default is \code{c(1:10, seq(12, 20, by = 2))}.}

\item{network_type}{Character string specifying network type.
Allowed values are "unsigned", "signed", "signed hybrid".
Default is "unsigned". Passed to the \verb{NetworkType`` argument of }WGCNA::blockwiseModules()`.}

\item{tom_type}{Character string specifying topological overlap matrix type.
Allowed values are "none", "unsigned", "signed".
Default is "unsigned". Passed to the \verb{TOMType`` argument of }WGCNA::blockwiseModules()`.}

\item{min_module_size}{Minimum module size for module detection.
Default is 30. Passed to the \verb{minModuleSize`` argument of }WGCNA::blockwiseModules()`.}

\item{deep_split}{Integer value between 0 and 4. Provides a simplified control
over how sensitive module detection should be to module splitting.
Default is 2. Passed to the \verb{deepSplit`` argument of }WGCNA::blockwiseModules()`.}

\item{merge_cut_height}{Dendrogram cut height for merging of modules.
Default is 0.25. Passed to the \verb{mergeCutHeight`` argument of }WGCNA::blockwiseModules()`.}

\item{add_info}{A logical value indicating whether to add variable information
to the modules tibble. Only applicable to \code{gly_wgcna()}.}

\item{...}{Additional arguments passed to \code{WGCNA::blockwiseModules()}.}

\item{expr_mat}{A numeric matrix with variables as rows and samples as columns.}
}
\value{
A list with two elements:
\itemize{
\item \code{tidy_result}: A list containing two tibbles:
\itemize{
\item \code{modules}: Module assignments and membership values for each variable
\item \code{eigenvalues}: Module eigenvalues (first principal component of module expression)
}
\item \code{raw_result}: The raw WGCNA blockwiseModules object
The list has classes \code{glystats_wgcna_res} and \code{glystats_res}.
}
}
\description{
Perform WGCNA analysis to identify co-expression modules and their eigenvalues.
The function uses WGCNA package to construct weighted gene co-expression networks,
detect modules, and calculate module membership and eigenvalues.
}
\details{
The function performs log2 transformation on the expression data (log2(x + 1)) before
WGCNA analysis.

\code{gly_wgcna()} is the top-level API that works with \code{glyexp::experiment()} objects and supports
the \code{add_info} parameter for joining experiment metadata.

\code{gly_wgcna_()} is the underlying API that works with matrices directly,
providing more flexibility for users who don't use the glyexp package.

\strong{Analysis Steps:}
\enumerate{
\item Soft threshold selection using \code{WGCNA::pickSoftThreshold()}
\item Network construction and module detection using \code{WGCNA::blockwiseModules()}
\item Module membership calculation based on correlation with module eigengenes
\item Results formatting into standardized tibbles
}
}
\section{Required packages}{

This function requires the following packages to be installed:
\itemize{
\item WGCNA` for weighted gene co-expression network analysis
}
}

\seealso{
\code{\link[WGCNA:pickSoftThreshold]{WGCNA::pickSoftThreshold()}}, \code{\link[WGCNA:blockwiseModules]{WGCNA::blockwiseModules()}}
}
