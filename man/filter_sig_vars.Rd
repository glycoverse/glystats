% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-sig-vars.R
\name{filter_sig_vars}
\alias{filter_sig_vars}
\title{Filter Significant Variables}
\usage{
filter_sig_vars(
  exp,
  res,
  p_adj_cutoff = 0.05,
  p_val_cutoff = NULL,
  fc_cutoff = NULL,
  comparison = NULL
)
}
\arguments{
\item{exp}{An \code{\link[glyexp:experiment]{glyexp::experiment()}}.}

\item{res}{A glystats result object from a glystats DEA function.}

\item{p_adj_cutoff}{The threshold for p-adjusted values. Default is 0.05.}

\item{p_val_cutoff}{The threshold for p-values. We don't recommend using this. Default is NULL.}

\item{fc_cutoff}{The threshold for fold changes. Default is NULL. Only positive value is needed.
For example, \code{2} means fold change > 2 or < 1/2.}

\item{comparison}{Only matters for multi-group DEA results.
Specifies which comparison to filter on. A string with the format "group1_vs_group2".
For \code{\link[=gly_anova]{gly_anova()}} and \code{\link[=gly_kruskal]{gly_kruskal()}} results, if \code{comparison} is NULL,
the main test results will be used, otherwise the post-hoc test results.
For \code{\link[=gly_limma]{gly_limma()}} results, if \code{comparison} is NULL, all comparisons will be filtered.}
}
\value{
An new \code{\link[glyexp:experiment]{glyexp::experiment()}} object.
}
\description{
Filtering the experiment to keep only significant variables is a common task.
This function provides a convenient way to do this.
It supports results from all glystats DEA functions including
\code{\link[=gly_anova]{gly_anova()}}, \code{\link[=gly_kruskal]{gly_kruskal()}}, \code{\link[=gly_ttest]{gly_ttest()}}, \code{\link[=gly_wilcox]{gly_wilcox()}}, and \code{\link[=gly_limma]{gly_limma()}}.
}
\examples{
library(glyexp)
library(glyclean)

exp <- auto_clean(real_experiment)
res <- gly_anova(exp)
sig_exp <- filter_sig_vars(exp, res)

}
