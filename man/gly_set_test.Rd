% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-test.R
\name{gly_set_test}
\alias{gly_set_test}
\title{Test Correlated Variable Sets}
\usage{
gly_set_test(
  exp,
  sets,
  group_col = "group",
  subject_col = NULL,
  method = "hotelling",
  p_adj_method = "BH"
)
}
\arguments{
\item{exp}{A \code{\link[glyexp:GlycomicSE]{glyexp::GlycomicSE()}} or \code{\link[glyexp:GlycoproteomicSE]{glyexp::GlycoproteomicSE()}} object,
or another \code{SummarizedExperiment} containing expression data and sample
information.}

\item{sets}{A \code{glystats_correlated_sets} object returned by
\code{\link[=gly_correlated_sets]{gly_correlated_sets()}}, or a uniquely named list of character vectors.}

\item{group_col}{A character string naming the two-level grouping column in
sample information. Default is \code{"group"}.}

\item{subject_col}{An optional character string naming subject identifiers.
When supplied, one-sample Hotelling tests are applied to paired
test-minus-reference difference vectors. Otherwise independent two-sample
Hotelling tests are used.}

\item{method}{Statistical method. Currently only \code{"hotelling"} is
supported.}

\item{p_adj_method}{A character string specifying the method used to adjust
set-level p-values. See \code{\link[stats:p.adjust]{stats::p.adjust()}}. Default is \code{"BH"}. If \code{NULL},
no adjustment is performed.}
}
\value{
A list with classes \code{glystats_set_test_res} and \code{glystats_res}
containing:
\itemize{
\item \code{tidy_result$sets}: One row per set with its estimate vector, Hotelling
statistic, degrees of freedom, Mahalanobis effect size, p-value, adjusted
p-value, and fit status.
\item \code{tidy_result$members}: One row per set member with its marginal estimate
and mean within-set correlation.
\item \code{raw_result}: Set definitions, their correlation matrix, and individual
Hotelling test objects.
\item \code{meta_data}: Metadata copied from \code{exp}.
}
}
\description{
Jointly test correlated variable sets with Hotelling's \eqn{T^2} statistic.
}
\details{
Expression values are transformed using \code{log2(x + 1e-6)}. The first factor
level of \code{group_col} is the reference group and the second is the test group.
The estimate is therefore \code{test - reference}.

Equivalent profiles are included in member-level output but represented once
in the covariance matrix. Sets that are too large for their residual sample
size or have a singular covariance matrix are retained with \code{status = "failed"}, \code{NA} statistics, and an explicit \code{failure_reason}.
}
\examples{
set.seed(1)
expression <- matrix(
  stats::rexp(36),
  nrow = 3,
  dimnames = list(c("A", "B", "C"), paste0("S", 1:12))
)
expression["B", ] <- expression["A", ] * exp(stats::rnorm(12, sd = 0.01))
exp <- SummarizedExperiment::SummarizedExperiment(
  assays = list(expression = expression),
  colData = S4Vectors::DataFrame(
    group = factor(
      rep(c("control", "case"), each = 6),
      levels = c("control", "case")
    )
  )
)
sets <- gly_correlated_sets(exp, threshold = 0.8)
result <- gly_set_test(exp, sets)
result$tidy_result$sets

}
\seealso{
\code{\link[=gly_correlated_sets]{gly_correlated_sets()}}, \code{\link[stats:p.adjust]{stats::p.adjust()}}
}
