% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{gly_pca}
\alias{gly_pca}
\title{Principal Component Analysis (PCA)}
\usage{
gly_pca(exp, center = TRUE, scale = TRUE, add_info = TRUE, ...)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} or \code{SummarizedExperiment} object containing
an expression matrix and sample information.}

\item{center}{A logical indicating whether to center the data. Default is TRUE.}

\item{scale}{A logical indicating whether to scale the data. Default is TRUE.}

\item{add_info}{A logical value. If TRUE (default), sample and variable information from the experiment
will be added to the result tibbles. If FALSE, only the PCA results are returned.}

\item{...}{Additional arguments passed to \code{prcomp()}.}
}
\value{
A list containing:
\itemize{
\item \code{tidy_result}: A list of tibbles with PCA results:
\itemize{
\item \code{samples}: PCA scores for each sample containing the following columns:
\itemize{
\item \code{sample}: Sample name
\item \code{PC}: Principal component name (PC1, PC2, etc.)
\item \code{value}: Score value for the principal component
}
\item \code{variables}: PCA loadings for each variable containing the following columns:
\itemize{
\item \code{variable}: Variable name
\item \code{PC}: Principal component name (PC1, PC2, etc.)
\item \code{value}: Loading value for the principal component
}
\item \code{eigenvalues}: PCA eigenvalues containing the following columns:
\itemize{
\item \code{PC}: Principal component name (PC1, PC2, etc.)
\item \code{std.dev}: Standard deviation
\item \code{percent}: Percentage of variance explained
\item \code{cumulative}: Cumulative percentage of variance explained
}
}
\item \code{raw_result}: The raw prcomp object from \code{stats::prcomp()}
\item \code{meta_data}: A list containing metadata from the input experiment
}
}
\description{
Perform principal component analysis on the expression data.
The function uses \code{prcomp()} to perform PCA and \code{broom::tidy()} to tidy the results.
If \code{scale = TRUE}, constant variables (zero variance) will be removed before PCA.
}
\details{
The function performs log transformation on the expression data (log(x + 1)) before
PCA analysis.
}
\section{Required packages}{

This function only uses base R packages and does not require additional dependencies.
}

\seealso{
\code{\link[stats:prcomp]{stats::prcomp()}}
}
