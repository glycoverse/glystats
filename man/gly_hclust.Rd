% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hclust.R
\name{gly_hclust}
\alias{gly_hclust}
\title{Hierarchical Clustering for Glycomics and Glycoproteomics Data}
\usage{
gly_hclust(
  exp,
  on = "variable",
  method = "complete",
  dist_method = "euclidean",
  k_values = c(2, 3, 4, 5),
  scale = TRUE,
  add_info = TRUE,
  return_raw = FALSE,
  ...
)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} object containing expression matrix and sample information.}

\item{on}{A character string specifying what to cluster. Either "variable" (default) to cluster
variables/features, or "sample" to cluster samples/observations.}

\item{method}{The agglomeration method to be used. This should be one of
"ward.D", "ward.D2", "single", "complete", "average", "mcquitty", "median", or "centroid".
Default is "complete".}

\item{dist_method}{The distance measure to be used. This must be one of
"euclidean", "maximum", "manhattan", "canberra", "binary", or "minkowski".
Default is "euclidean".}

\item{k_values}{A numeric vector specifying the number of clusters to cut the tree into.
Default is c(2, 3, 4, 5). If NULL, no cluster assignments are returned.}

\item{scale}{A logical indicating whether to scale the data before clustering. Default is TRUE.}

\item{add_info}{A logical value. If TRUE (default), sample information from the experiment
will be added to the result tibbles. If FALSE, only the clustering results are returned.}

\item{return_raw}{A logical value. If FALSE (default), returns processed tibble results.
If TRUE, returns raw hclust object.}

\item{...}{Additional arguments passed to \code{stats::dist()} or \code{stats::hclust()}.}
}
\value{
A list containing multiple tibbles (when return_raw = FALSE):
\itemize{
\item \code{clusters}: Cluster assignments for different k values (variables or samples depending on \code{on} parameter)
\item \code{dendrogram}: Dendrogram segment data for plotting (if ggdendro is available)
\item \code{heights}: Merge heights and steps for the clustering process
\item \code{labels}: Labels and their positions (if ggdendro is available)
When return_raw = TRUE, returns the raw hclust object.
}
}
\description{
Perform hierarchical clustering on the expression data.
The function uses \code{stats::hclust()} to perform clustering and provides
tidy results including cluster assignments, dendrogram data for plotting,
and merge heights.
}
\details{
The function performs log2 transformation on the expression data (log2(x + 1)) before
clustering. When \code{on = "variable"} (default), variables are clustered based on their
expression patterns across samples. When \code{on = "sample"}, samples are clustered based
on their expression profiles across variables.

\strong{Distance Calculation:}
Distance is calculated using \code{stats::dist()} with the specified method.

\strong{Clustering Method:}
Hierarchical clustering is performed using \code{stats::hclust()} with the specified method.

\strong{Cluster Assignment:}
The dendrogram is cut at different heights to produce cluster assignments for
the specified k values using \code{stats::cutree()}.
}
\section{Required packages}{

This function only uses base R packages and does not require additional dependencies.
For enhanced dendrogram plotting capabilities, the \code{ggdendro} package is recommended
but not required.
}

\seealso{
\code{\link[stats:hclust]{stats::hclust()}}, \code{\link[stats:dist]{stats::dist()}}, \code{\link[stats:cutree]{stats::cutree()}}
}
