% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oplsda.R
\name{gly_oplsda}
\alias{gly_oplsda}
\alias{gly_oplsda_}
\title{Orthogonal Partial Least Squares Discriminant Analysis (OPLS-DA)}
\usage{
gly_oplsda(
  exp,
  group_col = "group",
  pred_i = 1,
  ortho_i = NA,
  scale = TRUE,
  add_info = TRUE,
  ...
)

gly_oplsda_(expr_mat, groups, pred_i = 1, ortho_i = NA, scale = TRUE, ...)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} object containing expression matrix and sample information.}

\item{group_col}{A character string specifying the column name in sample information
that contains group labels. Default is "group".}

\item{pred_i}{An integer indicating the number of predictive components to include. Default is 1.}

\item{ortho_i}{An integer indicating the number of orthogonal components to include. Default is NA (automatic).}

\item{scale}{A logical indicating whether to scale the data. Default is TRUE.}

\item{add_info}{A logical value. If TRUE (default), sample and variable information from the experiment
will be added to the result tibbles. If FALSE, only the OPLS-DA results are returned.
Only applicable to \code{gly_oplsda()}.}

\item{...}{Additional arguments passed to \code{ropls::opls()}.}

\item{expr_mat}{A numeric matrix with variables as rows and samples as columns.}

\item{groups}{A factor or character vector specifying group membership for each sample.
Must have exactly 2 levels. Character vectors will be automatically converted to factors.}
}
\value{
A list containing:
\itemize{
\item \code{tidy_result}: A list of tibbles with OPLS-DA results:
\itemize{
\item \code{samples}: OPLS-DA scores for each sample containing the following columns:
\itemize{
\item \code{sample}: Sample name
\item \code{group}: Group assignment
\item \code{p1}, \code{p2}, etc.: Predictive component scores
\item \code{o1}, \code{o2}, etc.: Orthogonal component scores
}
\item \code{variables}: OPLS-DA loadings for each variable containing the following columns:
\itemize{
\item \code{variable}: Variable name
\item \code{p1}, \code{p2}, etc.: Predictive component loadings
\item \code{o1}, \code{o2}, etc.: Orthogonal component loadings
}
\item \code{variance}: OPLS-DA explained variance containing the following columns:
\itemize{
\item \code{component}: Component name (p1, o1, etc.)
\item \code{explained_variance}: Percentage of variance explained by each component
}
\item \code{vip}: Variable Importance in Projection scores containing the following columns:
\itemize{
\item \code{variable}: Variable name
\item \code{vip}: VIP score
}
}
\item \code{raw_result}: The raw ropls opls object from \code{ropls::opls()}
}
}
\description{
Perform orthogonal partial least squares discriminant analysis on the expression data.
The function uses \code{ropls::opls()} to perform OPLS-DA and returns tidy results.
}
\details{
\code{gly_oplsda()} is the top-level API that works with \code{glyexp::experiment()} objects and supports
the \code{add_info} parameter for joining experiment metadata.

\code{gly_oplsda_()} is the underlying API that works with matrices and factor vectors directly,
providing more flexibility for users who don't use the glyexp package.
}
\section{Required packages}{

This function requires the following packages to be installed:
\itemize{
\item \code{ropls} for OPLS-DA analysis
}
}

\section{Sample size requirements}{

According to the Topliss ratio principle, the ratio of samples to variables (n/p)
should be at least 5 to avoid overfitting and ensure reliable results. This function
will throw an error if n/p < 5. For datasets with high dimensionality relative to
sample size, consider:
\itemize{
\item Feature selection before analysis
\item Collecting more samples
}
}

\seealso{
\code{\link[ropls:opls]{ropls::opls()}}
}
