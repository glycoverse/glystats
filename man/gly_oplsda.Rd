% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oplsda.R
\name{gly_oplsda}
\alias{gly_oplsda}
\title{Orthogonal Partial Least Squares Discriminant Analysis (OPLS-DA)}
\usage{
gly_oplsda(
  exp,
  group_col = "group",
  pred_i = 1,
  ortho_i = NA,
  scale = TRUE,
  return_raw = FALSE,
  ...
)
}
\arguments{
\item{exp}{A \code{glyexp::experiment()} object containing expression matrix and sample information.}

\item{group_col}{A character string specifying the column name in sample information
that contains group labels. Default is "group".}

\item{pred_i}{An integer indicating the number of predictive components to include. Default is 1.}

\item{ortho_i}{An integer indicating the number of orthogonal components to include. Default is NA (automatic).}

\item{scale}{A logical indicating whether to scale the data. Default is TRUE.}

\item{return_raw}{A logical value. If FALSE (default), returns processed tibble results.
If TRUE, returns raw ropls opls object.}

\item{...}{Additional arguments passed to \code{ropls::opls()}.}
}
\value{
A list containing four tibbles (when return_raw = FALSE):
\itemize{
\item \code{samples}: OPLS-DA scores for each sample with group information
\item \code{variables}: OPLS-DA loadings for each variable
\item \code{variance}: OPLS-DA explained variance information
\item \code{vip}: Variable Importance in Projection (VIP) scores for each variable
When return_raw = TRUE, returns the raw ropls opls object.
}
}
\description{
Perform orthogonal partial least squares discriminant analysis on the expression data.
The function uses \code{ropls::opls()} to perform OPLS-DA and returns tidy results.
}
\section{Required packages}{

This function requires the following packages to be installed:
\itemize{
\item \code{ropls} for OPLS-DA analysis
}
}

\section{Sample size requirements}{

According to the Topliss ratio principle, the ratio of samples to variables (n/p)
should be at least 5 to avoid overfitting and ensure reliable results. This function
will throw an error if n/p < 5. For datasets with high dimensionality relative to
sample size, consider:
\itemize{
\item Feature selection before analysis
\item Collecting more samples
}
}

\seealso{
\code{\link[ropls:opls]{ropls::opls()}}
}
